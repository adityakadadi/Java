1.Write a java program:
a) To calculate and display the roots of Quadratic Equation.

code:-

package project;

import java.util.Scanner;
public class QuadraticEquationDemo {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.print("Enter a: ");
        double a = input.nextDouble();
        System.out.print("Enter b: ");
        double b = input.nextDouble();
        System.out.print("Enter c: ");
        double c = input.nextDouble();
        double discriminant = b * b - 4 * a * c;
         
        if (discriminant > 0) {
            System.out.println("The equation has real and distinct roots.");
            double root1 = (-b + Math.sqrt(discriminant)) / (2 * a);
            double root2 = (-b - Math.sqrt(discriminant)) / (2 * a);
            System.out.println("The roots are " + root1 + " and " + root2);
        } else if (discriminant == 0) {
            System.out.println("The equation has real and equal roots.");
            double root = -b / (2 * a);
            System.out.println("The root is " + root);
        } else {
            System.out.println("The equation has no real roots (complex roots).");
            double realp = -b / (2 * a);
            double imagp = Math.sqrt(-discriminant) / (2 * a);
            System.out.printf("The roots are (%.2f + i%.4f) and (%.2f - i%.4f)%n", realp, imagp, realp, imagp);
        }
        input.close();
    }
}

b) To multiply two arrays and display the result.
code:-

package project;

import java.util.Scanner;

public class Array{
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        System.out.print("Enter the number of elements in the first array: ");
        int n = input.nextInt();
        int[] array1 = new int[n];
        for (int i = 0; i < n; i++) {
            System.out.print("Enter element " + (i + 1) + " of the first array: ");
            array1[i] = input.nextInt();
        }

        System.out.print("Enter the number of elements in the second array: ");
        int m = input.nextInt();
        int[] array2 = new int[m];
        for (int i = 0; i < m; i++) {
            System.out.print("Enter element " + (i + 1) + " of the second array: ");
            array2[i] = input.nextInt();
        }

        if (n != m) {
            System.out.println("Error: Arrays have different lengths, not possible to multiply them.");
            input.close();
            return;
        }

        int[] result = new int[n];
        for (int i = 0; i < n; i++) {
            result[i] = array1[i] * array2[i];
        }

        System.out.print("Result of element-wise multiplication: ");
        for (int i = 0; i < n; i++) {
            System.out.print(result[i] + " ");
        }
        input.close();
    }
}

c) To sort the elements in ascending and descending order using bubble sort algorithm.
code:-

package project;

import java.util.Scanner;

public class BubbleSort {
    public static void bubbleSortAscending(int[] arr) {
        int n = arr.length;
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }
    }
 public static void bubbleSortDescending(int[] arr) {
	 int n = arr.length;
	 for (int i = 0; i < n - 1; i++) {
		 for (int j = 0; j < n - i - 1; j++) {
			 if (arr[j] < arr[j + 1]) {
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }
    }

    public static void printArray(int[] arr) {
        for (int num : arr) {
            System.out.print(num + " ");
        }
        System.out.println();
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter number of elements: ");
        int n = scanner.nextInt();
        int[] arr = new int[n];
        System.out.println("Enter " + n + " elements:");
        for (int i = 0; i < n; i++) {
            arr[i] = scanner.nextInt();
        }

        bubbleSortAscending(arr);
        System.out.println("\nSorted in Ascending Order:");
        printArray(arr);

        bubbleSortDescending(arr);
        System.out.println("\nSorted in Descending Order:");
        printArray(arr);

        scanner.close();
    }
}

