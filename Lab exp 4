Write a java program to define a base class called person which
stores information about a person such as name, age, gender. Derive
two new classes employee and student and extend the base class
methods in derived class to read and display the details that are
specific to student and employee. Display the details of at least five
students and five employees.

code:-

package project;

import java.util.Scanner;

class Person {
    String name;
    int age;
    String gender;

    void readData(Scanner sc) {
        System.out.print("Enter name: ");
        name = sc.nextLine();
        System.out.print("Enter age: ");
        age = Integer.parseInt(sc.nextLine());
        System.out.print("Enter gender: ");
        gender = sc.nextLine();
    }

    void displayData() {
        System.out.println("Name: " + name);
        System.out.println("Age: " + age);
        System.out.println("Gender: " + gender);
    }
}

class Employee1 extends Person {
    String employeeId;
    String department;
    double salary;

    void readData(Scanner sc) {
        super.readData(sc);
        System.out.print("Enter employee ID: ");
        employeeId = sc.nextLine();
        System.out.print("Enter department: ");
        department = sc.nextLine();
        System.out.print("Enter salary: ");
        salary = Double.parseDouble(sc.nextLine());
    }

    void displayData() {
        super.displayData();
        System.out.println("Employee ID: " + employeeId);
        System.out.println("Department: " + department);
        System.out.println("Salary: " + salary);
        System.out.println("-----------------------------------");
    }
}

class Student extends Person {
    String rollNo;
    String course;
    double marks;

    void readData(Scanner sc) {
        super.readData(sc);
        System.out.print("Enter roll number: ");
        rollNo = sc.nextLine();
        System.out.print("Enter course: ");
        course = sc.nextLine();
        System.out.print("Enter marks: ");
        marks = Double.parseDouble(sc.nextLine());
    }

    void displayData() {
        super.displayData();
        System.out.println("Roll Number: " + rollNo);
        System.out.println("Course: " + course);
        System.out.println("Marks: " + marks);
        System.out.println("-----------------------------------");
    }
}

public class PersonMain {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        Student[] students = new Student[5];
        Employee1[] employees = new Employee1[5];

        System.out.println("Enter details for 5 students:");
        for (int i = 0; i < 5; i++) {
            System.out.println("Student " + (i + 1) + ":");
            students[i] = new Student();
            students[i].readData(sc);
        }

        System.out.println("Enter details for 5 employees:");
        for (int i = 0; i < 5; i++) {
            System.out.println("Employee " + (i + 1) + ":");
            employees[i] = new Employee1();
            employees[i].readData(sc);
        }

        System.out.println("Student Details:");
        for (Student s : students) {
            s.displayData();
        }

        System.out.println("Employee Details:");
        for (Employee1 e : employees) {
            e.displayData();
        }

        sc.close();
    }
}
